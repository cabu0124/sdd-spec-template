# Skill — <name>

<!-- Template. Copy to `docs/skills/<slug>/SKILL.md` and fill <...> — one
directory per skill, so it can carry its own supporting files alongside
SKILL.md. `/sdd-onboard` copies it as-is for tools with a native skill
directory; tools without one skip it silently. -->

---
name: <slug>
description: <concrete trigger phrases and situations — this is what a model matches against to decide whether to load it, so be specific, not aspirational>
---

<Step-by-step guidance the model follows once this skill is loaded — e.g. how
to write a testable acceptance criterion, or structure a wireframe. Reference
any supporting file with a path relative to this skill's own directory.>
