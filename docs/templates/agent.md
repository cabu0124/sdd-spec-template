# Agent — <role>

<!-- Template. Copy to `docs/agents/<slug>.md` and fill <...>. One file per
role — product manager, business analyst, UX writer, domain expert — whatever
authoring this Spec Repository actually splits into. `/sdd-onboard` turns each
one into a subagent or chat mode for the tools that support one; tools that
don't skip it silently. -->

---
description: <when to reach for this role instead of the default agent — concrete situations, not a job title>
---

<What this role does and does not decide, 2-3 lines. It still answers to Rule 1
in `AGENTS.md` — a role is a lens on the same repository, not an exception to
"specs only, spec first".>

## Reads first

- `AGENTS.md` — Rule 1 still applies
- <files or docs this role needs before acting>

## Boundaries

- Never: <what this role must not do — e.g. write a plan, name a technology>
- Ask before: <...>
