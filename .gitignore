# Agent adapters + command files — generated per developer by
# docs/commands/onboard.md. They only point at AGENTS.md and carry no project
# content, so they are not shared. To share one: delete its line, commit it.
/CLAUDE.md
/GEMINI.md
/.claude/
/.cursor/
/.gemini/
/.codex/
/.github/copilot-instructions.md
/.github/prompts/
/.github/chatmodes/
/.github/skills/
/.github/instructions/
/.github/hooks/

# Full output of compacted runs, kept by scripts/sdd-recall.sh. Local, and it
# holds whatever the commands printed.
/.sdd/recall/
/**/*.DS_Store